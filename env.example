# Environment Variables for Layout Clustering Project
# Copy this file to .env and adjust values as needed

# Python path configuration
PYTHONPATH=.

# Logging configuration
LOG_LEVEL=INFO

# Model paths (adjust as needed)
# TRANSFORMERS_CACHE=./models/transformers
# TORCH_HOME=./models/torch

# OpenAI Configuration
# API key is required (no default in config files for security)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Override YAML configuration settings
# These will override values in config/recommendation_config.yaml
# OPENAI_MODEL=gpt-4.1-mini
# OPENAI_TEMPERATURE=0.7
# OPENAI_MAX_TOKENS=300

# Note: Most settings are now configured in config/recommendation_config.yaml
# Environment variables above provide runtime overrides when needed

# Other API keys (if needed)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Project-specific settings
PROJECT_NAME=layout-clustering-final
DATA_PATH=./data/01_raw/assignment_data.json

# Development settings
DEBUG=False
TESTING=False
